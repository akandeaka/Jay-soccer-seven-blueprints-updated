name: Troubleshoot Data Source

on:
  workflow_dispatch:

jobs:
  troubleshoot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run diagnostic
        run: |
          python troubleshoot.py
